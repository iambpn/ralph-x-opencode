{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    // Playwright MCP for browser automation - enabled by default
    "playwright": {
      "type": "local",
      "command": ["bun", "x", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
  // you can add more configurations here as needed
}
